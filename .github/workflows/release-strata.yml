name: 🚀 Release Strata

on:
  push:
    tags:
      - 'strata-v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag name (e.g. strata-v0.1.0-test)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.set-tag.outputs.tag }}
      version: ${{ steps.set-tag.outputs.version }}
    steps:
      - name: Set tag and version
        id: set-tag
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="${{ inputs.tag }}"
          else
            TAG="${GITHUB_REF#refs/tags/}"
          fi
          VERSION="${TAG#strata-v}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Create published release
        uses: softprops/action-gh-release@72f2c25fcb47643c292f7107632f7a47c1df5cd8 # v2.3.2
        with:
          tag_name: ${{ steps.set-tag.outputs.tag }}
          name: Strata ${{ steps.set-tag.outputs.version }}
          draft: false
          prerelease: ${{ contains(steps.set-tag.outputs.tag, '-test') || contains(steps.set-tag.outputs.tag, '-rc') || contains(steps.set-tag.outputs.tag, '-beta') }}
          generate_release_notes: true
          body: |
            # Strata ${{ steps.set-tag.outputs.version }}

            Strata — il GIS con l'AI nativa. Fork di QGIS con AI integrata: parla con le tue mappe, lancia agenti geospaziali multi-step e interroga tutto il workspace.

            > An AI-native fork of QGIS. Chat with your maps, run multi-step geospatial agents, and query your entire workspace — right inside the desktop.

            ## Download

            | Sistema | File | Note |
            |---|---|---|
            | **macOS** (Intel + Apple Silicon) | `*.dmg` | Universal binary, **firmato Apple Developer ID + notarizzato**. Doppio click sul DMG, trascina l'app in Applications, lancia. |
            | **Windows** (x64) | `*-win64.exe`, `*-win64.zip` | Installer NSIS e ZIP portable. Gli asset di release sono firmati con Sigstore keyless e checksum SHA256; se Authenticode è configurato, anche i binari Windows sono firmati. |
            | **Linux** (x86_64) | `*.AppImage`, `*.AppImage.sigstore.json`, `*.AppImage.sha256` | AppImage firmata con Sigstore keyless e checksum SHA256. `chmod +x` poi doppio click. |

            > Su Windows la reputazione SmartScreen può richiedere alcuni download reali prima di stabilizzarsi.

            ## Configurazione AI

            Al primo avvio: **View → Panels → AI Assistant**, click sull'icona ⚙ in alto a destra,
            incolla la tua API key OpenAI o Anthropic. Le chiavi sono salvate nelle impostazioni
            locali dell'applicazione e non vengono mai inviate a server diversi dal provider scelto.
