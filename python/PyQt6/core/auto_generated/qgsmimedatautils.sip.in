/************************************************************************
 * This file has been generated automatically from                      *
 *                                                                      *
 * src/core/qgsmimedatautils.h                                          *
 *                                                                      *
 * Do not edit manually ! Edit header and run scripts/sipify.py again   *
 ************************************************************************/





class QgsMimeDataUtils
{
%Docstring(signature="appended")
Contains utility functions for working with MIME data.
%End

%TypeHeaderCode
#include "qgsmimedatautils.h"
%End
  public:
    struct Uri
    {
        Uri();
%Docstring
Constructs invalid URI
%End
        explicit Uri( const QString &encData );
%Docstring
Constructs URI from encoded data
%End

        explicit Uri( QgsMapLayer *layer );
%Docstring
Constructs a URI corresponding to the specified ``layer``.

.. versionadded:: 3.8
%End

        bool isValid() const;
%Docstring
Returns whether the object contains valid data
%End

        QString data() const;
%Docstring
Returns encoded representation of the object
%End

 QgsVectorLayer *vectorLayer( bool &owner /Out/, QString &error /Out/ ) const /Deprecated="Since 4.4. Use vectorLayer() with explicit project parameter instead."/;
%Docstring
Gets vector layer from uri if possible, otherwise returns ``None`` and
error is set



:return: - vector layer if successful, otherwise ``None``
         - owner: set to ``True`` if caller becomes owner
         - error: set to error message if cannot get vector

.. deprecated:: 4.4

   Use :py:func:`~QgsMimeDataUtils.vectorLayer` with explicit project parameter instead.
%End

        QgsVectorLayer *vectorLayer( bool &owner /Out/, QString &error /Out/, QgsProject *project ) const;
%Docstring
Gets vector layer from uri if possible, otherwise returns ``None`` and
error is set

:param project: project is used to obtain the layer

:return: - vector layer if successful, otherwise ``None``
         - owner: set to ``True`` if caller becomes owner
         - error: set to error message if cannot get vector

.. versionadded:: 4.4
%End

 QgsRasterLayer *rasterLayer( bool &owner /Out/, QString &error /Out/ ) const /Deprecated="Since 4.4. Use rasterLayer() with explicit project parameter instead."/;
%Docstring
Gets raster layer from uri if possible, otherwise returns ``None`` and
error is set



:return: - raster layer if successful, otherwise ``None``
         - owner: set to ``True`` if caller becomes owner
         - error: set to error message if cannot get raster

.. deprecated:: 4.4

   Use :py:func:`~QgsMimeDataUtils.rasterLayer` with explicit project parameter instead.
%End

        QgsRasterLayer *rasterLayer( bool &owner /Out/, QString &error /Out/, QgsProject *project ) const;
%Docstring
Gets raster layer from uri if possible, otherwise returns ``None`` and
error is set

:param project: project is used to obtain the layer

:return: - raster layer if successful, otherwise ``None``
         - owner: set to ``True`` if caller becomes owner
         - error: set to error message if cannot get raster

.. versionadded:: 4.4
%End

 QgsMeshLayer *meshLayer( bool &owner /Out/, QString &error /Out/ ) const /Deprecated="Since 4.4. Use meshLayer() with explicit project parameter instead."/;
%Docstring
Gets mesh layer from uri if possible, otherwise returns ``None`` and
error is set



:return: - mesh layer if successful, otherwise ``None``
         - owner: set to ``True`` if caller becomes owner
         - error: set to error message if cannot get mesh

.. deprecated:: 4.4

   Use :py:func:`~QgsMimeDataUtils.meshLayer` with explicit project parameter instead.
%End

        QgsMeshLayer *meshLayer( bool &owner /Out/, QString &error /Out/, QgsProject *project ) const;
%Docstring
Gets mesh layer from uri if possible, otherwise returns ``None`` and
error is set

:param project: project is used to obtain the layer

:return: - mesh layer if successful, otherwise ``None``
         - owner: set to ``True`` if caller becomes owner
         - error: set to error message if cannot get mesh

.. versionadded:: 4.4
%End

 QgsMapLayer *mapLayer() const /Deprecated="Since 4.4. Use mapLayer() with explicit project parameter instead."/;
%Docstring
Returns the layer from the active project corresponding to this uri (if
possible), otherwise returns ``None``.

Unlike :py:func:`~QgsMimeDataUtils.vectorLayer`,
:py:func:`~QgsMimeDataUtils.rasterLayer`, or
:py:func:`~QgsMimeDataUtils.meshLayer`, this method will not attempt to
create a new layer corresponding to the URI.

:return: map layer if found in the active project, otherwise ``None``

.. versionadded:: 3.8

.. deprecated:: 4.4

   Use :py:func:`~QgsMimeDataUtils.mapLayer` with explicit project parameter instead.
%End

        QgsMapLayer *mapLayer( QgsProject *project ) const;
%Docstring
Returns the layer from ``project`` corresponding to this uri (if
possible), otherwise returns ``None``.

Unlike :py:func:`~QgsMimeDataUtils.vectorLayer`,
:py:func:`~QgsMimeDataUtils.rasterLayer`, or
:py:func:`~QgsMimeDataUtils.meshLayer`, this method will not attempt to
create a new layer corresponding to the URI.

:param project: used to obtain the layer

:return: map layer if found in the project, otherwise ``None``

.. versionadded:: 4.4
%End

        QString layerType;

        QString providerKey;

        QString name;
        QString uri;
        QStringList supportedCrs;
        QStringList supportedFormats;

        QString layerId;

        QString pId;

        Qgis::WkbType wkbType;

        QString filePath;

      SIP_PYOBJECT __repr__();
%MethodCode
      QString str = QStringLiteral( "<QgsMimeDataUtils::Uri (%1): %2>" ).arg( sipCpp->providerKey, sipCpp->uri );
      sipRes = PyUnicode_FromString( str.toUtf8().constData() );
%End
    };
    typedef QList<QgsMimeDataUtils::Uri> UriList;

    static QMimeData *encodeUriList( const UriList &layers ) /Factory/;
%Docstring
Encodes a URI list to a new QMimeData object.
%End

    static bool isUriList( const QMimeData *data );

    static UriList decodeUriList( const QMimeData *data );

    static QByteArray layerTreeNodesToUriList( const QList<QgsLayerTreeNode *> &nodes );
%Docstring
Returns encoded URI list from a list of layer tree nodes.
%End

    static bool hasOriginatedFromCurrentAppInstance( const QgsMimeDataUtils::Uri &uri );
%Docstring
Returns ``True`` if ``uri`` originated from the current QGIS application
instance.

.. versionadded:: 3.8
%End

};


/************************************************************************
 * This file has been generated automatically from                      *
 *                                                                      *
 * src/core/qgsmimedatautils.h                                          *
 *                                                                      *
 * Do not edit manually ! Edit header and run scripts/sipify.py again   *
 ************************************************************************/
