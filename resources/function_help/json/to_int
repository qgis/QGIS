{
  "name": "to_int",
  "type": "function",
  "groups": ["Conversions"],
  "description": "Rounds a numeric value to integer or converts a string to integer number. Nothing is returned if the value cannot be converted to integer (e.g '123asd' is invalid).",
  "arguments": [{
    "arg": "string",
    "description": "string or numeric value to convert to integer number"
  }],
  "examples": [{
    "expression": "to_int(123.45)",
    "returns": "123"
  }, {
    "expression": "to_int(123.67)",
    "returns": "124"
  }, {
    "expression": "to_int('123')",
    "returns": "123"
  }],
  "tags": ["convert", "integer", "string", "round", "ceil", "floor"]
}
